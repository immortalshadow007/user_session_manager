"""
Django settings for session_management project.

Generated by 'django-admin startproject' using Django 5.1.1.

For more information on this file, see
https://docs.djangoproject.com/en/5.1/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/5.1/ref/settings/
"""

import os
import warnings
from pathlib import Path
from dotenv import load_dotenv
from mongoengine import connect
from cryptography.utils import CryptographyDeprecationWarning

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# Load environment variables from the custom .env file
env_file_path = BASE_DIR / '.env.mongodb.django'
env_database_file_path = BASE_DIR / '.env.mongodb.db_store'
env_azure_file_path = BASE_DIR / '.env.azure.credentials'
env_JWT_file_path = BASE_DIR / '.env.jwt_storage'
env_psql_file_path = BASE_DIR / '.env.psql_store'

# Load Django environment variables
if os.path.exists(env_file_path):
    load_dotenv(env_file_path)
else:
    print(f"Environment file {env_file_path} does not exist")

# Load database environment variables
if os.path.exists(env_database_file_path):
    load_dotenv(env_database_file_path)
else:
    print(f"Database environment file {env_database_file_path} does not exist")

# Load Azure credentials environment variables
if os.path.exists(env_azure_file_path):
    load_dotenv(env_azure_file_path)
else:
    print(f"Azure credentials environment file {env_azure_file_path} does not exist")

# Load PSQL environment variables
if os.path.exists(env_psql_file_path):
    load_dotenv(env_psql_file_path)
else:
    print(f"PSQL environment variable file {env_psql_file_path} does not exist")

# Load JWT environment variable
if os.path.exists(env_JWT_file_path):
    load_dotenv(env_JWT_file_path)
else:
    print(f"JWT environment file {env_JWT_file_path} does not exist")
JWT_SECRET_KEY = os.getenv('JWT_SECRET_KEY')
if not JWT_SECRET_KEY:
    raise ValueError("No JWT_SECRET_KEY set for JWT token encoding/decoding")

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/5.1/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = os.getenv('DJANGO_SECRET_KEY')
if not SECRET_KEY:
    raise ValueError("No DJANGO_SECRET_KEY set for Django application")

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = os.getenv('DJANGO_DEBUG', 'False') == 'False'

ALLOWED_HOSTS = [
    'localhost',
    '127.0.0.1',
    '[::1]',
]

# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'session_manager',
    'django_extensions',
    'rest_framework',
    'channels',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'session_manager.middleware.SessionMiddleware',
]

CORS_ALLOWED_ORIGINS = [
    'http://localhost:3000',
    'https://127.0.0.1:3000',
    'http://localhost',
    'https://localhost:3000',
]

CORS_ALLOW_HEADERS = [
    'A-API-KEY',
    'Authorization',
    'Content-Type',
]

CORS_ALLOW_CREDENTIALS = True

CORS_ALLOW_METHODS = [
    'GET',
    'POST',
    'OPTIONS',
]

CSRF_COOKIE_HTTPONLY = False

ROOT_URLCONF = 'session_management.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

ASGI_APPLICATION = 'session_management.asgi.application'


# Database
# https://docs.djangoproject.com/en/5.1/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.getenv('POSTGRES_DB_NAME'),
        'USER': os.getenv('POSTGRES_USER'),
        'PASSWORD': os.getenv('POSTGRES_PASSWORD'),
        'HOST': os.getenv('POSTGRES_HOST', 'localhost'),
        'PORT': os.getenv('POSTGRES_PORT', '5432'),
    }
}

# MongoDB URIs
MONGO_DB_URI = os.getenv('MONGO_DB_URI')
MONGO_DB_NAME_VERIFICATION = os.getenv('MONGO_DB_NAME_VERIFICATION')

# MongoDB Database name
Profile_db = os.getenv('USER_PROFILE_DB')

try:
    connect (
        db = Profile_db,
        host = os.getenv('MONGO_DB_URI'),
        alias = 'user_profile_db',
        connectTimeoutMS = 5000,
        socketTimeoutMS = 5000,
        serverSelectionTimeoutMS = 5000
    )
except Exception as e:
    print(f'Failed to connect to MongoDB (user_profile). Error: {e}')
    raise

try:
    connect(
        db=os.getenv('MONGO_DB_NAME_VERIFICATION'),
        host=os.getenv('MONGO_DB_URI'),
        alias="verification_db",  # Alias for the verification database
        connectTimeoutMS=5000,
        socketTimeoutMS=5000,
        serverSelectionTimeoutMS=5000
    )
except Exception as e:
    print(f"Failed to connect to MongoDB (verification). Error: {e}")
    raise

# Suppress CryptographyDeprecationWarning warnings
warnings.filterwarnings("ignore", category=CryptographyDeprecationWarning)

# Logging Configuration
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters': {
        'verbose': {
            'format': '{levelname} {asctime} {module} {message}',
            'style': '{',
        },
        'simple': {
            'format': '{levelname} {message}',
            'style': '{',
        },
    },
    'filters': {
        'ignore_deprecation_warnings': {
            '()': 'django.utils.log.CallbackFilter',
            'callback': lambda record: not isinstance(record.exc_info, tuple) or not issubclass(record.exc_info[0], CryptographyDeprecationWarning),
        },
    },
    'handlers': {
        'console': {
            'level': 'INFO',
            'class': 'logging.StreamHandler',
            'formatter': 'verbose',
        },
        'file': {
            'level': 'INFO',
            'class': 'logging.FileHandler',
            'filename': os.path.join(BASE_DIR, 'django.log'),
            'formatter': 'verbose',
        },
    },
    'root': {
        'handlers': ['console', 'file'],
        'level': 'INFO',
    },
    'loggers': {
        'django': {
            'handlers': ['console', 'file'],
            'level': 'INFO',
            'propagate': False,
        },
        'mobile_number': {
            'handlers': ['console', 'file'],
            'level': 'INFO',
            'propagate': False,
        },
        'azure': {
            'handlers': ['console', 'file'],
            'level': 'WARNING',  # Change this to WARNING or ERROR
            'propagate': False,
        },
        'azure.core': {
            'handlers': ['console', 'file'],
            'level': 'WARNING',  # Change this to WARNING or ERROR
            'propagate': False,
        },
        'azure.identity': {
            'handlers': ['console', 'file'],
            'level': 'WARNING',  # Change this to WARNING or ERROR
            'propagate': False,
        },
    },
}

# Password validation
# https://docs.djangoproject.com/en/5.1/ref/settings/#auth-password-validators
'''
AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]
'''

# Internationalization
# https://docs.djangoproject.com/en/5.1/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/5.1/howto/static-files/

STATIC_URL = 'static/'

# Default primary key field type
# https://docs.djangoproject.com/en/5.1/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
